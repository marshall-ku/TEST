name: Publish package

on:
    release:
        types: [created]
    repository_dispatch:

jobs:
    publish-npm:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - run: echo 'Hello world'
